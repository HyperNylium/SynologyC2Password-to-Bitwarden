name: Lint test

on:
  push:
    branches: [ master ]
    paths:
      - "*.py"
      - "**/*.py"
  pull_request:
    branches: [ master ]
    paths:
      - "*.py"
      - "**/*.py"

jobs:
  ruff-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v9.0.0
        with:
          python-version: '3.11'
      - run: uv run ruff check .

  pytest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v9.0.0
        with:
          python-version: '3.11'
      - run: uv run pytest
